# Copy to .env only if you need to override the defaults.
# The defaults below match docker-compose.yml, so `npm run db:up` is enough.

DATABASE_URL=postgres://lysa:lysa@localhost:5433/lysa
PORT=4000
